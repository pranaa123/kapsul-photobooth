import "./guest.css";
import {GuestCamera} from "@/features/camera/guest-camera";

export const metadata={title:"Rania & Dava — Kapsul",robots:{index:false,follow:false}};
export default function GuestEventPage(){return <GuestCamera/>}
