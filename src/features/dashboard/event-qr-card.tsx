"use client";import {useState} from "react";import {Check,Copy,ExternalLink} from "lucide-react";import {QRCodeSVG} from "qrcode.react";
export function EventQrCard({slug,token}:{slug:string;token:string}){const[copied,setCopied]=useState(false);const base=typeof window!=="undefined"?window.location.origin:(process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000");const url=`${base}/e/${slug}?t=${token}`;async function copy(){await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <article className="event-qr-card"><div><span className="dash-kicker">TAUTAN TAMU</span><h2>QR acaramu</h2><p>Bagikan setelah acara aktif. QR ini tidak membuka galeri privat.</p><button onClick={copy}>{copied?<Check/>:<Copy/>}{copied?"Tersalin":"Salin tautan"}</button><a href={url} target="_blank">Preview halaman tamu <ExternalLink/></a></div><div className="real-qr"><QRCodeSVG value={url} size={150} bgColor="#ffffff" fgColor="#11110f" level="M"/></div></article>}
